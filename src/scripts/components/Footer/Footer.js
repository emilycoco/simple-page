import React, { Component } from 'react';
import styles from './Footer.styl';

export class Footer extends Component {
    render() {
        return (
            <footer className="footer content-container top-margin">The Marketplace is a work in progress</footer>
        )
    }
}